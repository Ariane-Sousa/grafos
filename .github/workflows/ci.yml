name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Build
      run: |
        docker-compose build

    - name: Teste Pytest and Lint
      run: |
        docker-compose run --rm web pytest
        docker-compose run --rm web lint